# wrangler files
.wrangler
.dev.vars*
!.dev.vars.example
.env*
!.env.example

# macOS
.DS_Store

# Local deploy scripts — they contain your GitHub token. NEVER publish them.
*.command

# Local reference + archive — not part of the deployable game.
# NOTE: unlike Sudoku/Racimo we do NOT ignore *.png here — Pasos's
# index.html references cortadito-orange.png, Logo Orange transparent.png
# and pasos-footprints.png at the root, so those must ship.
_reference/
_archive/
